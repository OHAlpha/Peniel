<%= form_for(@bible_lesson_category) do |f| %>
  <% if @bible_lesson_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bible_lesson_category.errors.count, "error") %> prohibited this bible_lesson_category from being saved:</h2>

      <ul>
      <% @bible_lesson_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
